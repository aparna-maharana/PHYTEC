/* WACP to find ASCII value of a character, take character from user. */
#include<stdio.h>
int main(){
    char v;
    printf("Enter the Character %c\n",v);
    scanf("%c",&v);
    printf("Value %d\n",v);
}