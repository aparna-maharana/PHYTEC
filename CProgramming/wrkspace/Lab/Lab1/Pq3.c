/* WACP that accepts two integers from the user and calculate the sum of two integers. */
#include <stdio.h>
int main()
{
    int a,b;
    scanf("%d", &a);
    scanf("%d", &b);
    printf("%d", a+b);
}