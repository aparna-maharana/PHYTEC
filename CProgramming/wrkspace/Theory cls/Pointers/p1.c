/* The address of pointer and the value of a variable is same */
printf("value of a is %d",a);
printf("Address of a is:%d", &a);
printf("Value of p is:%d", p);
printf("Address of p is:%d", &p);/* Shows the address of the pointer */
/* Using pointer read the value of a  */
printf("value of a using pointer: %d", *p) 
*p =  *(1000) /* pointing the value of p */
   = 10 /* Value of 1000 is 10 */
   int *ip = For integer
   int *f = For float
   int *cp = For character